package javaforgit;

public class LoopPG {
    public static void main(String[] args) {
        int num = 5;
        for (int i = 0; i < 4; i++) {

                }
        // for each loop created in the dev branch on the remote level
        String str = "Hello";

        // for each loop created in the dev branch on the remote level
            }
        }
